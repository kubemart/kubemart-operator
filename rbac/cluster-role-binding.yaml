apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: bizaar-daemon-role-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: bizaar-daemon-role
subjects:
  - kind: ServiceAccount
    name: bizaar-daemon-svc-acc
    namespace: default # TODO - have this in another namespace?
